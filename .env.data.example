# Simulacrax Local Dataset Configuration
# Copy this file to .env.data and fill in your paths.
# .env.data is gitignored — never commit actual paths or secrets.
#
# Loaded automatically by activate.sh after the GPU/CPU environment is set up.
# Usage: source ./activate.sh   (sources .env, then .env.data if present)

# Waymo Open Dataset: path to motion v1.2.1 TFRecord files
# WOD_MOTION_TFRECORD_PATH=/path/to/waymo/motion_v1.2.1/tf_example

# (Add other local dataset paths or API keys below)
